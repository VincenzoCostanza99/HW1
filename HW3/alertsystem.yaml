apiVersion: v1
kind: Service
metadata:
  name: alert-system
  namespace: homework3
  labels:
    app: alert-system
spec:
  selector:
    app: alert-system
  clusterIP: None  # Comunicazione interna basata su DNS
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-system
  namespace: homework3
  labels:
    app: alert-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-system
  template:
    metadata:
      labels:
        app: alert-system
    spec:
      containers:
      - name: alert-system
        image: quay.io/vincenzo_cost/alertsystem_homework3:v1
        imagePullPolicy: Always